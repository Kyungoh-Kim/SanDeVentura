# SanDeVentura — 앱 기능 및 활용 방안

> SanDeVentura는 **사용자가 기록한 GPS 세션을 공유 등산 경로로 변환**하는 크라우드소싱 등산 안내 플랫폼입니다.
> 모바일 앱(등산객)과 웹 대시보드(운영자)로 구성됩니다.

---

## 구성 요소 한눈에 보기

| 구성 요소 | 대상 사용자 | 핵심 역할 |
|---|---|---|
| 모바일 앱 (Flutter / Android) | 등산객 | GPS 기록 · 경로 안내 · 데이터 기여 |
| 웹 운영자 대시보드 (React) | 서비스 운영자 | 경로 품질 모니터링 · 커버리지 관리 |

---

## 1. 모바일 앱

### 1-1. Record 탭 — GPS 세션 기록

**기능**

| 기능 | 설명 |
|---|---|
| 세션 시작/종료 | 산 선택 후 기록 시작. 언제든 종료 가능 |
| 오프라인 기록 | 네트워크 없이도 SQLite에 GPS 포인트 로컬 저장 |
| 라이브 지도 | 실시간 이동 경로를 지도에 표시 (flutter_map) |
| 자동 중심 토글 | 지도가 내 위치를 자동으로 따라갈지 수동 탐색할지 전환 |
| 줌 인/아웃 | 지도 확대·축소 버튼 |
| 지도 레이어 전환 | 일반 지도(OSM) ↔ 위성 이미지(Esri) |
| 앱 재시작 복구 | 앱이 종료되어도 진행 중인 세션을 자동으로 복구 |
| 업로드 큐 | 와이파이 연결 시 세션 자동 업로드 대기열 관리 |

**활용 방안**

- **비공식 코스 데이터 기여**: 공식 앱에 없는 코스를 등산하며 기록하면 해당 경로의 canonical trail 구축에 기여됩니다.
- **오프라인 산행**: 산 정상·음영 지역에서도 GPS 기록이 중단 없이 이어집니다.
- **세션 누적으로 경로 품질 향상**: 같은 코스를 여러 사람이 반복 기록할수록 경로 안내의 정확도가 자동으로 향상됩니다.

---

### 1-2. Route 탭 — 실시간 경로 안내

**기능**

| 기능 | 설명 |
|---|---|
| 경로 미리보기 지도 | 산에 등록된 canonical trail(들)을 지도에 polyline으로 표시 |
| 다중 경로 표시 | 한 산의 여러 코스를 동시에 표시, 신뢰도에 따라 색상 구분 |
| 자동 중심 토글 | 내 위치를 지도 중심으로 자동 추적하거나 수동 탐색 전환 |
| 줌 인/아웃 | 지도 확대·축소 버튼 |
| 산 전체 보기 | 등록된 모든 경로가 화면에 맞게 자동 축소/확대 |
| 지도 레이어 전환 | 일반 지도 ↔ 위성 이미지 |
| 산 변경 | 상단 선택기에서 등록된 다른 산으로 즉시 전환 |
| snap-position 비교 | 현재 내 위치와 경로의 거리를 서버에서 계산해 반환 |
| 경로 상태 카드 | 활성 경로의 신뢰도·버전·업데이트 일시 표시 |
| 스냅 결과 카드 | 경로와의 거리, 경로 판정(경로 위 / 주의 / 이탈) 표시 |

**경로 신뢰도 색상**

| 색상 | 신뢰도 등급 | 의미 |
|---|---|---|
| 초록 | `recommended` | 다수 세션이 검증한 안정적 경로 |
| 주황 | `reference` | 참고용 경로 (데이터 부족) |
| 회색 | `none` | 경로 데이터 미구축 |

**snap-position 판정 기준**

| 판정 | 거리 | 의미 |
|---|---|---|
| On route | ≤ 25 m | 경로 위 정상 진행 |
| Caution | 26–50 m | 경로 근처이나 주의 필요 |
| Away from route | > 50 m | 경로 이탈 가능성 |

**활용 방안**

- **갈림길 실시간 판단**: 등산 중 갈림길에서 snap-position을 호출해 현재 위치가 올바른 경로 위인지 즉시 확인할 수 있습니다.
- **경로 이탈 조기 감지**: 이탈 경보를 받으면 잘못 들어선 직후에 복귀 가능해, 조난 위험을 크게 줄입니다.
- **복수 코스 비교**: 같은 산의 여러 등산로를 지도에서 한눈에 비교하고 오늘 갈 코스를 선택할 수 있습니다.

---

### 1-3. Sessions 탭 — 업로드 관리

**기능**

| 기능 | 설명 |
|---|---|
| 세션 목록 조회 | 로컬에 저장된 세션 목록과 상태(기록 중 / 완료 / 업로드됨) 확인 |
| 업로드 큐 상태 | 업로드 대기 중인 세션의 진행 상태 및 재시도 횟수 확인 |
| 수동 업로드 트리거 | 자동 업로드가 실패한 경우 수동으로 재시도 |

**활용 방안**

- **데이터 기여 현황 확인**: 내가 기록한 세션이 서버에 성공적으로 전송됐는지 확인합니다.
- **업로드 실패 대응**: 산 속 오프라인 구간에서 기록한 세션을 하산 후 수동으로 업로드합니다.

---

## 2. 웹 운영자 대시보드

운영자가 서비스 전체의 경로 품질과 사용 현황을 모니터링하는 내부 도구입니다.

### 2-1. Overview 페이지

**기능**

- 전체 세션 수, 등록 산 수, 누적 GPS 포인트 수 요약 지표
- 최근 업로드 세션 목록

**활용 방안**

- 서비스 전반의 데이터 축적 속도를 한눈에 파악합니다.

---

### 2-2. Mountains 페이지

**기능**

| 기능 | 설명 |
|---|---|
| 산 목록 | 등록된 모든 산과 코스 수, 세션 수 확인 |
| 산 bbox 미리보기 | 선택한 산의 경계 영역을 지도에 시각화 |
| 커버리지 현황 | 산별 canonical trail 구축 현황 |

**활용 방안**

- 어느 산의 데이터가 부족한지 파악하고, 해당 산 사용자에게 더 많은 기록을 독려할 우선순위를 결정합니다.

---

### 2-3. Routes 페이지

**기능**

| 기능 | 설명 |
|---|---|
| 경로 목록 | 산별 등록 경로와 신뢰도 등급 조회 |
| 경로 지도 시각화 | canonical trail geometry를 OpenLayers 지도에 표시 |
| 신뢰도 지표 | confidence 점수, 참여 세션 수, GPS 품질 점수 확인 |
| 지도 레이어 전환 | 일반 지도 ↔ 위성 이미지 |

**활용 방안**

- 경로 geometry가 실제 등산로와 일치하는지 위성 이미지와 겹쳐 육안으로 검증합니다.
- 신뢰도가 낮은 경로(`reference` / `none`)를 우선 확인하고 추가 세션 수집 대상을 선정합니다.

---

### 2-4. Sessions 페이지

**기능**

| 기능 | 설명 |
|---|---|
| 업로드 히스토리 | 모든 세션의 상태(업로드됨 / 처리됨 / 거부됨) 타임라인 |
| 세션별 포인트 수 | 수락된 포인트 수 vs 거부된 포인트 수 |
| 파이프라인 진행 상태 | uploaded → accepted → ingested 단계 모니터링 |

**활용 방안**

- 특정 기간에 업로드가 비정상적으로 적거나 거부율이 높은 경우를 탐지합니다.
- 세션이 경로 추론 파이프라인의 어느 단계에 머물러 있는지 추적합니다.

---

### 2-5. Quality 페이지

**기능**

| 기능 | 설명 |
|---|---|
| 경로별 품질 지표 | 세션 수, 포인트 밀도, 평균 GPS 정확도, 거부 포인트 비율 |
| 신뢰도 점수 분해 | confidence 계산 인자별 기여도 확인 |
| 이상치 탐지 | 거부율 이상 급등, 정확도 저하 구간 식별 |

**품질 점수 구성 요소**

| 인자 | 가중치 | 의미 |
|---|---|---|
| 세션 수 | 35% | 데이터 기반의 두께 |
| GPS 품질 | 20% | 평균 수신 정확도 |
| 전이 일관성 | 15% | 경로 반복 일관성 |
| 분기 모호성 | 15% | 갈림길 경로 결정력 |
| 거부율 | 10% | 이상 포인트 비율 |
| 최신성 | 5% | 최근 세션 반영 여부 |

**활용 방안**

- GPS 정확도가 낮은 구간(수목 밀집, 계곡)을 식별하고 임계값을 조정합니다.
- 거부 포인트가 집중된 경로를 찾아 재추론(recompute) 트리거 여부를 결정합니다.

---

### 2-6. Discovery 페이지

**기능**

| 기능 | 설명 |
|---|---|
| 후보 셀 클러스터 조회 | 아직 경로로 확정되지 않은 GPS 밀집 구간 표시 |
| 신규 경로 후보 탐지 | 등록된 경로 없이 반복 통과된 구역 시각화 |
| 경로 생성 트리거 | 후보 클러스터를 새 canonical trail로 승격 |

**활용 방안**

- 사용자들이 자연스럽게 반복 통행하는 미등록 구역을 발견하고 새 경로를 추가합니다.
- 특정 산에 경로가 없어도 세션이 쌓이면 Discovery 페이지에서 자동으로 후보 경로가 제안됩니다.

---

## 3. 전체 흐름 요약

```
[등산객] 모바일 앱으로 산행 기록
    │
    ▼
GPS 포인트 → 로컬 SQLite (오프라인 버퍼)
    │
    ▼
와이파이 연결 시 자동 업로드 → Supabase 검증 + 저장
    │
    ▼
match-and-aggregate 파이프라인
    └── H3 그리드 셀 기반 경로 추론
    └── canonical trail 생성 / 갱신 (신뢰도 점수 재계산)
    │
    ├─▶ [등산객] Route 탭 경로 안내 품질 향상
    │       └── snap-position: 경로 이탈 실시간 감지
    │
    └─▶ [운영자] 웹 대시보드 모니터링
            └── Routes / Quality / Discovery 페이지
```

---

## 4. 사용자 유형별 활용 포인트

### 단독 산행 등산객
- Record 탭으로 기록하며 데이터 기여 → Route 탭으로 갈림길 이탈 즉시 감지
- 비공식 코스도 몇 번 다니면 경로 안내가 생성됨

### 등산 동호회
- 회원 다수가 같은 코스를 기록 → 빠른 canonical trail 구축 (세션 10~20개면 신뢰도 0.8 도달)
- 동호회 전용 코스를 앱 데이터로 체계화 가능

### 서비스 운영자
- 대시보드로 경로 품질을 수치로 관리
- GPS 오차 이슈 → 임계값 조정 → 재추론 전 과정을 대시보드에서 완결
- Discovery 페이지로 신규 등록 대상 경로를 데이터 기반으로 발굴
