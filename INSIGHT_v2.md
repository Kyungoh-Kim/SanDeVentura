# SanDeVentura — 인사이트 및 개선 방향 v2

> 현재 MVP 구조를 비판적으로 검토하고, 우선순위 있는 개선 방향을 제시한다.  
> Sprint 9 기준으로 작성. 세션-경로 귀속(attribution) 시스템, 분기 감지, candidate 경로 파이프라인 포함.

---

## 우선순위 요약

| 순위 | 문제 | 영향도 | 구현 난이도 |
|------|------|--------|-------------|
| 1 | 콜드 스타트 — seed 데이터 없음 | 매우 높음 | 낮음 |
| 2 | 사용자 획득 전략 없음 | 높음 | 중간 |
| 3 | GPS 정확도 한계 | 높음 | 중간 |
| 4 | 테스트 체계 부재 | 중간 | 낮음 |
| 5 | 운영자 단일 의존 구조 | 중간 | 중간 |
| 6 | 프라이버시 오버슈팅 | 낮음 | 낮음 |

---

## 1. 콜드 스타트 — 가장 치명적인 구조적 약점

### 문제

SanDeVentura의 핵심 가치인 "경로 안내"는 canonical trail이 존재해야 동작한다. 그러나 canonical trail을 만들려면 같은 산을 다수의 사용자가 반복 방문해야 한다.

- 경로 신뢰도 0.8 달성에 필요한 추정 세션 수: 10~20개
- 활성 사용자 1명의 동일 산 방문 빈도: 월 1~2회
- 신뢰도 0.8 달성까지의 최소 기간: 5~10개월 (단독 사용자 기준)

**결과**: 초기 사용자는 "경로 데이터 없음"만 보다가 앱을 삭제할 가능성이 높다.

현재 `recompute-canonical-trails`는 수동으로도 트리거 가능하지만, 세션 자체가 없으면 의미가 없다.

### 개선 방향

**단기 (ROI 최고)**:
- 인기 등산로(북한산 백운대, 설악산 대청봉 등) 공개 GPX 데이터(국가산림공단, OpenStreetMap) 기반 seed canonical trail 구성
- `confidence_level = 'reference'` + "참고용 경로, 사용자 데이터 반영 전" UI 레이블 명시
- `promote-candidate-cluster` Edge Function을 GPX import에 재활용 가능

**중기**:
- 운영자가 GPX 파일을 직접 업로드해 canonical trail을 수동 초기화하는 기능
- 사용자 기여 세션이 쌓이면 seed를 사용자 데이터로 점진적 교체 (가중평균 병합)

---

## 2. 사용자 획득 전략 없음 — 기술보다 중요한 문제

### 문제

선순환 구조(사용자 → 데이터 → 경로 품질 향상 → 신규 사용자 유입)가 작동하려면 초기 기여자가 있어야 한다. 현재 PRD와 구현은 기술에 집중되어 있고 GTM 전략이 없다.

- 등산 커뮤니티(산사랑, 클라이밍 커뮤니티) 파트너십 미정
- 초기 seed 사용자 모집 방법 미정
- "내가 기여한 경로가 다른 사람에게 도움이 됐다"는 피드백 루프 미구현

### 개선 방향

**단기**:
- "내 기여 현황" 화면: "당신의 데이터가 N개 세션 생성에 기여했습니다"
- 특정 등산 동호회/커뮤니티 1~2곳과 파트너십 → 초기 데이터 기여자 확보

**중기**:
- `session_route_assignments`를 활용한 기여 기록 통계 화면
- 경로 기여자 배지 또는 커뮤니티 공개 기여 순위 (게이미피케이션 최소화)
- 오픈소스 데이터 기여 모델 고려 (내 GPS 데이터를 공공 등산 DB에도 기여)

---

## 3. GPS 정확도 한계 — 핵심 기능의 신뢰성 위협

### 문제

snap-position 임계값은 25m(경로 위) / 50m(이탈)로 설정되어 있다. 그러나:

- 수목 밀도 높은 산림 구간: GPS 오차 15~30m 일반적
- 깊은 계곡, 암벽 옆: GPS 오차 30~50m까지 증가
- **25m 임계값이 GPS 오차 자체와 겹치는 상황**: 경로 위에 있어도 이탈 경고 발생

`validation.ts`의 `low_accuracy` 임계값(현재 100m)은 이 문제와 독립적이다. 수락된 포인트라도 18~22m 오차는 경로 geometry를 실제 등산로에서 수십 m 벗어나게 만들 수 있다.

### 개선 방향

**단기**:
- snap-position에 이동 평균(moving average) 적용: 1포인트가 아닌 최근 5포인트 평균 좌표로 판단
- `rejected_track_points` 데이터 활용 → 구간별 GPS 품질 지도 생성 → "이 구간은 GPS 오차가 큰 구간입니다" UI 알림

**중기**:
- 기압계(barometer) 기반 고도 데이터 융합 (Android API 지원) → 수직 이동 패턴으로 GPS 오차 보정
- Kalman filter 도입 → `track_points` 저장 전 노이즈 사전 제거
- 산별 GPS 품질 맵을 자동 생성해 임계값을 경로 구간별로 동적 조정

---

## 4. 테스트 체계 부재 — 누적되는 기술 부채

### 문제

현재 코드베이스에는 자동화 테스트가 사실상 없다.

- `route_inference.ts` (~24KB, 핵심 알고리즘): 단위 테스트 없음
- `validation.ts` 검증 로직 변경 시 회귀 위험 감지 불가
- `route_split_detection.ts` (분기 감지): 복잡한 엣지 케이스가 많지만 테스트 없음
- Edge Function 로직은 로컬 Supabase에서 수동 테스트에 의존

Sprint 9 이후 시스템이 복잡해질수록 이전 기능의 회귀 가능성이 누적된다.

### 개선 방향

**단기 (ROI 최고)**:
- `route_inference.ts`: 입력/출력 기반 단위 테스트 5~10개
  - 알려진 GPS 트레이스 → 예상 canonical trail 검증
  - 최소 세션(1개) 케이스, 다수 세션(20개+) 케이스
- `validation.ts`: 경계값 케이스 (accuracy: 99m, 100m, 101m; speed: 14.9, 15.0, 15.1m/s)
- `route_split_detection.ts`: 감지 조건 경계값 테스트

**중기**:
- Deno test runner로 Edge Function integration test
- CI/CD에 자동 테스트 파이프라인 추가 (`.github/workflows`)
- Flutter: `recording_controller` 단위 테스트, `upload_queue_service` 재시도 로직 테스트

---

## 5. 운영자 단일 의존 구조 — 확장성과 거버넌스 부재

### 문제

모든 품질 판단(임계값 조정, recompute 트리거, 경로 분할 승인)이 운영자 한 명에게 집중되어 있다.

- canonical trail의 오류를 사용자가 신고할 방법 없음
- 경로 오류가 발생해도 운영자가 알기 전까지 모든 사용자가 오경보를 받음
- `route_split_audit`으로 분기 감지 힌트는 생겼지만 여전히 운영자 개입 필요

### 개선 방향

**단기**:
- 모바일 앱에 "이 경로가 이상합니다" 신고 버튼 추가
  → `mvp_events` 테이블에 기록, 운영자 대시보드에 신고 카운트 노출

**중기**:
- 신뢰 사용자(파워유저)에게 경로 검토 권한 부여 (커뮤니티 검수 모델)
- `session_route_assignments`의 기여 데이터를 기반으로 고기여 사용자 식별
- 자동 분할 조건 강화 → 운영자 개입 없이 더 많은 케이스 자동 처리

---

## 6. 프라이버시 오버슈팅 — 운영 효율 저해

### 문제

"운영자는 raw 경로 열람 불가" 원칙은 옳지만, 현재 구현이 지나치게 보수적이다.

- canonical trail 이상 발생 시 어느 구간이 문제인지 지리적으로 특정하기 어려움
- `rejected_track_points` 좌표는 DB에 있지만 대시보드에서 시각화 불가
- 오류 원인 분석에 불필요하게 오랜 시간 소요

집계 수준의 지리 시각화(H3 셀 단위)는 개인정보 침해가 아니다.

### 개선 방향

**단기**:
- 운영자 대시보드에 "GPS 품질 열지도(heatmap)" 추가
  → H3 resolution 11 셀 단위 집계 (개별 사용자 식별 불가)
  → "이 구간에서 거부 포인트 집중" 시각화
  → Discovery 페이지의 candidate 셀 시각화와 동일 접근 방식

**중기**:
- 개인정보 보호 영향 평가(PIA) 기준으로 데이터 노출 정책 명문화
- 집계 레벨(H3 셀 단위) vs 원시 레벨(좌표 단위) 접근 권한 분리

---

## Sprint 9 이후 달라진 점

Sprint 9에서 완성된 **세션-경로 귀속(attribution) 시스템**이 미래 개선의 기반이 된다.

| 기능 | Sprint 9 전 | Sprint 9 후 |
|------|-------------|-------------|
| 미매칭 세션 | 폐기 | `candidate_cells`에 누적 |
| 분기 신호 | cross-route로 폐기 | `route_to_candidate_transitions`에 보존 |
| 분기 감지 | 없음 | `route_split_detection.ts` + `evaluate-route-splits` |
| 신규 경로 발굴 | 수동 | Discovery 페이지에서 candidate 클러스터 시각화 |
| 기여 추적 | 없음 | `session_route_assignments.contributing_sessions[]` |

이 시스템이 있기 때문에 #1(콜드 스타트), #5(운영자 의존) 문제를 해결하기 위한 도구가 이미 갖춰져 있다. 다음 스프린트에서는 기술 구현보다 **seed 데이터 확보와 커뮤니티 파트너십**에 집중해야 한다.

---

## 단기 핵심 액션 (다음 스프린트 추천)

1. **인기 코스 GPX seed 데이터 import 파이프라인 구축** — 콜드 스타트 해결
2. **`route_inference.ts` 단위 테스트 5개 작성** — 핵심 알고리즘 회귀 방지
3. **모바일 앱에 "내 기여 현황" 화면 추가** — 사용자 참여 동기 부여
4. **snap-position 이동 평균 필터 적용** — GPS 정확도 한계 완화
