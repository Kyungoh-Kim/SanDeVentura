# SanDeVentura — 앱 기능 및 활용 방안 v2

> SanDeVentura는 **사용자가 기록한 GPS 세션을 공유 등산 경로로 자동 변환**하는 크라우드소싱 등산 안내 플랫폼입니다.  
> 사용자가 산을 오를수록 시스템이 스스로 학습하며 경로 안내의 정확도가 높아집니다.

---

## 구성 요소 한눈에 보기

| 구성 요소 | 대상 | 핵심 역할 |
|-----------|------|-----------|
| 모바일 앱 (Flutter / Android) | 등산객 | GPS 기록 · 경로 안내 · 데이터 기여 |
| 웹 운영자 대시보드 (React) | 운영자 | 경로 품질 모니터링 · 커버리지 관리 · 신규 경로 발굴 |

---

## 1. 모바일 앱

### 1-1. Record 탭 — GPS 세션 기록

등산 중 GPS 위치를 로컬에 기록한다. 네트워크 없이도 완전히 동작하며, 앱이 강제 종료되어도 세션이 유실되지 않는다.

#### 기능 목록

| 기능 | 동작 설명 |
|------|-----------|
| **산 선택** | 등록된 산 목록에서 선택 (검색 모달 지원) |
| **세션 시작 / 종료** | Start 버튼으로 기록 시작, Stop 버튼으로 세션 완료 |
| **오프라인 GPS 기록** | 네트워크 없이도 SQLite에 포인트 저장 (5m 이동 시마다) |
| **실시간 지도 표시** | 현재 이동 궤적을 flutter_map 폴리라인으로 표시 |
| **자동 중심 토글** | 내 위치 자동 추적 ↔ 자유 지도 탐색 전환 |
| **지도 레이어 전환** | OSM 일반 지도 ↔ Esri 위성 이미지 |
| **앱 재시작 복구** | 앱 종료/기기 재부팅 후에도 진행 중 세션 자동 복구 |
| **업로드 자동 대기** | 세션 완료 시 업로드 큐에 자동 등록, Wi-Fi 연결 시 전송 |

#### 저장되는 GPS 데이터

| 필드 | 설명 |
|------|------|
| `recorded_at` | 포인트 수신 시각 (ISO 8601) |
| `lat`, `lon` | 위경도 |
| `altitude` | 고도 (m) |
| `accuracy` | GPS 정확도 반경 (m, 낮을수록 정확) |
| `speed` | 이동 속도 (m/s) |
| `sequence_index` | 세션 내 순서 번호 (중복 방지) |

#### 활용 방안

- **비공식 코스 데이터 기여**: 공식 앱에 없는 샛길, 비공식 등산로를 기록하면 canonical trail 구축에 자동 기여된다.
- **오프라인 산행**: 정상·음영 지역에서도 GPS 기록이 중단 없이 이어진다.
- **데이터 누적 효과**: 같은 코스를 여러 사람이 반복 기록할수록 경로 안내의 정확도가 자동 향상된다. 10~20개 세션 누적 시 신뢰도 0.8 이상 도달 가능.

---

### 1-2. Route 탭 — 실시간 경로 안내

현재 있는 산의 등산로(canonical trail)를 지도에 표시하고, 내 위치와 경로의 거리를 서버에서 계산해 실시간으로 경로 이탈 여부를 알린다.

#### 기능 목록

| 기능 | 동작 설명 |
|------|-----------|
| **경로 미리보기 지도** | 산에 등록된 모든 canonical trail을 폴리라인으로 표시 |
| **다중 경로 동시 표시** | 한 산의 여러 코스를 신뢰도 색상으로 구분하여 동시 표시 |
| **자동 최근접 경로 선택** | 모든 경로에 동시에 snap 요청 → 거리 가장 짧은 경로를 자동으로 활성화 |
| **snap-position 비교** | 현재 위치와 경로의 거리를 서버에서 계산 (PostGIS 기반) |
| **경로 상태 카드** | 활성 경로의 신뢰도·버전·업데이트 일시 표시 |
| **스냅 결과 카드** | 경로와의 거리 및 경로 판정 표시 |
| **자동 중심 토글** | 내 위치 자동 추적 ↔ 자유 탐색 |
| **지도 레이어 전환** | OSM ↔ 위성 이미지 |
| **산 전체 보기** | 등록된 모든 경로가 화면에 맞게 자동 축소/확대 |
| **산 변경** | 상단 선택기에서 즉시 다른 산으로 전환 |

#### 신뢰도 색상

| 색상 | 등급 | 의미 |
|------|------|------|
| 초록 | `recommended` | 5개 이상 세션, GPS 품질 양호, 분기 명확 — 안심하고 따를 수 있는 경로 |
| 주황 | `reference` | 데이터 부족 또는 조건 미달 — 참고용, 실제 경로와 차이가 있을 수 있음 |
| 회색 | `none` | 아직 경로 데이터 없음 |

#### snap-position 판정 기준

| 판정 | 거리 | 의미 |
|------|------|------|
| On route | ≤ 25m | 경로 위 정상 진행 |
| Caution | 26~50m | 경로 근처이나 주의 필요 |
| Away from route | > 50m | 경로 이탈 가능성 높음 |

#### 활용 방안

- **갈림길 실시간 판단**: 갈림길에서 snap-position 결과로 현재 경로가 올바른지 즉시 확인.
- **조기 이탈 감지**: 이탈 경보를 받으면 잘못 들어선 직후에 복귀 가능 → 조난 위험 사전 차단.
- **복수 코스 비교**: 오늘 갈 코스를 지도에서 신뢰도 기준으로 비교 선택.

---

### 1-3. Sessions 탭 — 업로드 관리

로컬에 저장된 세션 목록과 업로드 상태를 확인하고, 실패한 업로드를 수동으로 재시도한다.

#### 기능 목록

| 기능 | 동작 설명 |
|------|-----------|
| **세션 목록 조회** | 저장된 세션의 상태 (기록 중 / 완료 / 업로드됨) 일람 |
| **업로드 큐 상태** | 업로드 대기 중인 세션의 진행 상태, 재시도 횟수 |
| **수동 업로드 트리거** | 자동 업로드 실패 시 수동 재시도 |
| **세션 삭제** | 스와이프로 불필요한 로컬 세션 삭제 |

#### 세션 상태 흐름

```
recording → completed → queued → uploading → uploaded
                                      ↕
                               (실패 시 재시도)
```

#### 서버 처리 상태

```
ingesting → ingested (처리 대기)
          → rejected (전부 거부됨)
```

#### 활용 방안

- **기여 현황 확인**: 내가 기록한 세션이 서버에 전송됐는지 확인.
- **오프라인 후 업로드**: 하산 후 Wi-Fi 연결 시 자동 또는 수동 업로드.

---

## 2. 웹 운영자 대시보드

운영자가 서비스 전반의 경로 품질과 사용 현황을 모니터링하고 관리하는 내부 도구. 등산객에게는 노출되지 않는다.

### 2-1. Overview 페이지

서비스 전반의 데이터 축적 속도를 한눈에 파악한다.

| 지표 | 설명 |
|------|------|
| 전체 세션 수 | 업로드된 세션 총계 |
| 등록 산 수 | 데이터가 있는 산의 수 |
| 누적 GPS 포인트 수 | 수락된 포인트 총계 |
| 최근 업로드 세션 | 최근 순으로 나열된 세션 타임라인 |

---

### 2-2. Mountains 페이지

어느 산의 데이터가 부족한지 파악하고 우선순위를 결정한다.

| 기능 | 설명 |
|------|------|
| 산 목록 | 등록된 모든 산, 코스 수, 세션 수 |
| bbox 지도 미리보기 | 선택한 산의 경계 영역을 OpenLayers 지도에 표시 |
| 커버리지 현황 | 산별 canonical trail 구축 상태 |

**활용 방안**: 세션이 적은 산을 식별 → 해당 산 사용자에게 데이터 기여 독려 우선순위 결정.

---

### 2-3. Routes 페이지

경로 geometry가 실제 등산로와 일치하는지 시각적으로 검증한다.

| 기능 | 설명 |
|------|------|
| 경로 목록 | 산별 등록 경로와 신뢰도 등급 조회 |
| canonical trail 지도 시각화 | 경로 LineString을 OpenLayers에 표시 |
| 위성 이미지 오버레이 | 경로와 실제 등산로 육안 비교 |
| 신뢰도 지표 | confidence 점수, 세션 수, GPS 품질 |

**활용 방안**: 경로가 실제 등산로와 어긋나거나 신뢰도가 낮은(`reference`) 경로를 우선 확인 → recompute 트리거 결정.

---

### 2-4. Sessions 페이지

업로드 파이프라인 전 단계를 모니터링한다.

| 기능 | 설명 |
|------|------|
| 업로드 히스토리 | 세션 상태 타임라인 (uploaded → ingested → complete) |
| 세션별 포인트 수 | 수락/거부 포인트 수 비교 |
| 파이프라인 진행 상태 | 어느 단계에서 세션이 멈춰 있는지 추적 |

**활용 방안**: 특정 기간에 거부율이 급등하는 경우 감지 → 검증 임계값 조정 검토.

---

### 2-5. Quality 페이지

경로별 신뢰도를 인자 수준으로 분해해서 어떤 요소가 문제인지 진단한다.

| 지표 | 설명 |
|------|------|
| `confidence` | 0~1의 종합 신뢰도 점수 |
| `session_count` | 기여 세션 수 |
| `gps_quality_score` | 평균 GPS 정확도 기반 점수 |
| `branch_ambiguity_score` | 분기 모호성 (0에 가까울수록 명확) |
| `rejected_point_rate` | 거부 포인트 비율 |
| `latest_evidence_at` | 가장 최근 세션 기여 시점 |

#### 신뢰도 가중치 구조

| 항목 | 가중치 | 만점 조건 |
|------|--------|-----------|
| 세션 수 | 35% | 5개 이상 |
| GPS 품질 | 20% | accuracy → 0m (이론상) |
| 전이 일관성 | 15% | 모든 세션이 동일 경로 |
| 분기 명확성 | 15% | 분기점에서 한쪽이 압도적 |
| 거부율 | 10% | 거부 포인트 없음 |
| 최신성 | 5% | 30일 이내 세션 |

**활용 방안**: GPS 정확도가 낮은 구간(수목 밀집, 계곡) 식별 → 임계값 완화 → recompute 트리거.

---

### 2-6. Discovery 페이지

등록된 경로 없이 반복 통과된 미등록 구역을 탐지하고 신규 경로를 추가한다.

| 기능 | 설명 |
|------|------|
| candidate 셀 클러스터 | 75m 반경 내 기존 경로가 없는 GPS 밀집 구간 표시 |
| 신규 경로 후보 탐지 | 5개 이상의 미매칭 셀이 쌓인 산 목록 |
| 경로 생성 버튼 | promote-candidate-cluster 실행 → 새 canonical trail 생성 |
| 분기 감지 힌트 | route_split_audit 기록 기반 경로 분할 제안 |
| Execute split 버튼 | 분기를 단일 트랜잭션으로 실행 (split_route_atomic) |

**활용 방안**:
- 사용자들이 자연스럽게 반복 통과하는 미등록 구간을 데이터 기반으로 발굴.
- 경로 분기 감지 힌트를 확인하고 적절한 시점에 경로 분할 실행.

---

## 3. 전체 데이터 흐름

```
[등산객] Record 탭으로 산행 기록
    │
    ▼
GPS 포인트 → SQLite (오프라인 버퍼)
    │
    ▼ (Wi-Fi 연결 시)
upload-session: 검증(포인트 필터) + 저장
    │
    ▼ (pg_cron, 15분마다)
match-and-aggregate-sessions
    ├── H3 셀 변환 (resolution 11, ~25m)
    ├── 75m 반경으로 기존 경로에 귀속
    ├── 미매칭 셀 → candidate_cells 누적
    └── Transition 4분류 + DB 누적
    │
    ▼
recomputeRouteConfidence
    └── canonical_trails 버전 갱신
    │
    ├─▶ [등산객] Route 탭
    │       ├── get-canonical-trail: 최신 경로 GeoJSON
    │       └── snap-position: 경로 이탈 실시간 감지
    │
    ├─▶ [운영자] 대시보드
    │       ├── Routes / Quality: 경로 품질 모니터링
    │       └── Discovery: 신규 경로 후보 발굴 + 분기 분할
    │
    └─▶ (pg_cron, 1시간마다)
            evaluate-route-splits: 분기 자동 감지 + 분할
```

---

## 4. 사용자 유형별 핵심 가치

### 단독 산행 등산객

- Record 탭으로 기록하며 데이터 기여
- Route 탭으로 갈림길에서 실시간 경로 판정 → 조난 위험 감소
- 비공식 코스도 몇 번 다니면 경로 안내 생성

### 등산 동호회 (집단 기여)

- 동호회원 다수가 같은 코스 기록 → 빠른 canonical trail 구축
- 10~20개 세션 누적 시 신뢰도 0.8+ 도달
- 동호회 전용 코스를 자동으로 체계화

### 서비스 운영자

- 대시보드로 경로 품질을 수치로 관리
- GPS 오차 이슈 → 임계값 조정 → recompute → 대시보드 검증
- Discovery 페이지로 신규 등록 대상 경로를 데이터 기반으로 발굴
- evaluate-route-splits로 분기 경로 자동 분리

---

## 5. GPS 포인트 검증 규칙

업로드 시 각 포인트는 서버에서 검증된다. 아래 조건에 해당하면 `rejected_track_points`에 사유와 함께 기록된다.

| 거부 사유 | 조건 |
|-----------|------|
| `missing_recorded_at` | 시각 정보 없음 |
| `invalid_lat` | 위도가 −90~90 범위 벗어남 |
| `invalid_lon` | 경도가 −180~180 범위 벗어남 |
| `missing_sequence_index` | 순서 번호 없음 |
| `duplicate_sequence_index` | 동일 세션 내 순서 번호 중복 |
| `implausible_speed` | 속도 > 15m/s (54km/h) |
| `low_accuracy` | accuracy > 100m |

- 수락 포인트 1개 이상 → 세션 상태 `ingested` (경로 추론 대기)
- 전부 거부 → 세션 상태 `rejected`
