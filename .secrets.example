# SanDeVentura parent workspace secret template.
#
# Copy to .secrets/local.secrets or another ignored secret file when a local
# workflow needs credentials. Never commit real tokens, passwords, JWTs, service
# role keys, private keys, or signing secrets.

EXAMPLE_SECRET_VALUE=replace-me
